# WEBBOOK_STANDARD.md

## 1. 목적

이 문서는 여러 프로젝트에서 제작되는 Markdown 원고를 하나의 일관된 웹북 체계로 발행하고 장기적으로 관리하기 위한 공통 표준이다.

핵심 목표는 다음과 같다.

> **원고를 만드는 프로젝트와 웹북을 발행하는 프로젝트를 분리하고, 각 프로젝트에서 만들어진 다양한 Markdown 파일을 웹북 발행 단계에서 자동으로 표준화한다.**

책마다 웹사이트를 새로 만들거나, 원본 파일을 일일이 다시 편집하거나, 파일명을 사람이 반복해서 정리하는 방식을 피한다.

웹북 수가 늘어나더라도 같은 방식으로 발행·추가·수정·백업할 수 있는 구조를 유지한다.

---

# 2. 전체 구조

기본 발행 흐름은 다음과 같다.

```text
각 연구·해설 프로젝트
Chat / Work
        ↓
원본 Markdown
        ↓
웹북 발행 프로젝트
발행용 구조 정규화
        ↓
Google Drive
발행 기준본·백업
        ↓
GitHub
웹북 데이터 + 웹사이트
        ↓
GitHub Pages 등으로 발행
        ↓
Notion
웹북 도서관·프런트 페이지
```

각 서비스의 역할은 다음과 같이 구분한다.

---

## 3. 원고 제작 프로젝트와 웹북 프로젝트를 분리한다

각 책이나 사상가별 프로젝트에서는 **콘텐츠를 만드는 데 집중한다.**

예:

```text
잡아함경 프로젝트
중아함경 프로젝트
융 프로젝트
베일리 프로젝트
호킨스 프로젝트
선불교 프로젝트
```

여기에서 만들어진 Markdown 파일은 **원본 원고**로 취급한다.

원본 프로젝트에서는 웹북 전체의 파일명 규칙이나 URL 구조까지 억지로 맞출 필요가 없다.

그 프로젝트에 이미 확정된 파일명·번호·제목 규칙이 있다면 그대로 유지한다.

---

# 4. 원본 파일과 발행 파일을 구분한다

웹북 시스템에서는 다음 두 종류의 파일을 구분한다.

### ① 원본 Markdown

각 연구·해설 프로젝트에서 만들어진 파일이다.

예:

```text
『글래머_ 세계 문제』 제4부 「융합의 기법」__「4.6 융합과 ‘퍼스낼리티의 소멸’의 정확한 의미」.md
```

또는:

```text
중아함경 제1경 선법경 최종정제본.md
```

이 파일은 해당 프로젝트의 원본이므로 웹북 발행을 위해 임의로 개명하거나 수정하지 않는다.

### ② 발행용 Markdown

웹북 프로젝트에서 사용하는 복사본이다.

발행 단계에서 웹북 표준에 맞게 파일명만 정규화할 수 있다.

예:

```text
04-06_융합과_퍼스낼리티의_소멸의_정확한_의미.md
```

또는:

```text
0001_선법경.md
```

**본문 내용은 원본과 동일하게 유지한다.**

---

# 5. 핵심 원칙 — 원본을 다시 정제하지 않는다

다른 프로젝트에서 제작된 Markdown을 웹북으로 가져올 때 다음과 같이 처리한다.

```text
원본 MD
↓
내용 확인
↓
발행용 복사본 생성
↓
발행 파일명 표준화
↓
book.yaml 등록
↓
웹북 발행
```

원본 파일 자체를 다시 열어서 파일명을 수작업으로 고치는 과정을 기본 작업으로 만들지 않는다.

즉:

> **원본은 보존하고, 발행본을 정규화한다.**

이를 웹북 발행의 기본 원칙으로 한다.

---

# 6. 원본 파일명이 이미 적절하면 그대로 사용할 수 있다

원본 파일명이 이미 웹북 표준과 잘 맞는 경우에는 불필요하게 바꾸지 않는다.

예:

```text
01_선법경.md
02_주도수경.md
03_성유경.md
```

이런 파일은 그대로 발행용으로 사용할 수 있다.

반면 다음처럼 지나치게 긴 파일명은:

```text
『글래머_ 세계 문제』 제4부 「융합의 기법」__「4.6 융합과 ‘퍼스낼리티의 소멸’의 정확한 의미」.md
```

발행용 복사본에서:

```text
04-06_융합과_퍼스낼리티의_소멸의_정확한_의미.md
```

처럼 정규화한다.

---

# 7. Google Drive의 역할

Google Drive는 웹북 발행 기준본과 백업을 보관하는 서고로 사용한다.

권장 구조:

```text
웹북 데이터셋/
│
├─ 00_원본수집/
├─ 01_발행준비/
├─ 02_발행본/
└─ 99_백업/
```

또는 분야별로:

```text
웹북 데이터셋/
│
├─ 불교/
│  ├─ 초기경전/
│  │  ├─ 잡아함경/
│  │  └─ 중아함경/
│  └─ 선불교/
│
├─ 기독교/
├─ 융/
├─ 호킨스/
├─ 베일리/
└─ 기타/
```

실제 웹북에는 **발행 대상으로 확정된 파일만 사용한다.**

---

# 8. GitHub의 역할

GitHub는 다음 두 역할을 담당한다.

1. 웹북의 실제 Markdown 데이터 관리
2. 독자가 읽는 웹북 사이트 발행

가능하면 웹북 전체를 하나의 통합 저장소에서 관리한다.

예:

```text
webbooks/
│
├─ books/
│  ├─ buddhism/
│  ├─ christianity/
│  ├─ jung/
│  ├─ hawkins/
│  └─ bailey/
│
├─ site/
└─ README.md
```

이미 공개된 기존 저장소나 URL이 있다면 무리하게 통합하지 않는다.

**기존 공개 링크 보존을 새 구조 도입보다 우선한다.**

---

# 9. Notion의 역할

Notion은 웹북 본문의 중복 저장소가 아니라 **독자가 웹북을 발견하고 접근하는 도서관 프런트 페이지**로 사용한다.

Notion에는 주로 다음을 둔다.

- 책 제목
- 분야
- 시리즈
- 간단한 소개
- 연재 중 / 완결
- 웹북 바로가기
- 필요한 경우 데이터셋·원본 정보

GitHub 웹북 주소가 유지되는 경우, 장을 추가할 때마다 Notion 페이지를 다시 만들지 않는다.

---

# 10. 전체 도서관 계층

권장 구조:

```text
웹북 도서관
│
├─ 불교
│  ├─ 초기경전 쉬운 해설
│  │  ├─ 잡아함경
│  │  ├─ 중아함경
│  │  ├─ 장아함경
│  │  └─ 증일아함경
│  │
│  ├─ 선불교
│  └─ 불교 입문
│
├─ 기독교
│  ├─ 예수와 복음서
│  └─ 기독교 신비주의
│
├─ 심리학
│  └─ C. G. 융
│
├─ 영성·철학
│  ├─ David R. Hawkins
│  └─ Alice Bailey
│
└─ 기타
```

---

# 11. 한 웹북 내부의 기본 계층

자료에 따라 다음 계층을 사용한다.

```text
책
↓
부·권
↓
장·경·절
↓
본문·해설
```

모든 단계가 반드시 필요한 것은 아니다.

일반 저작:

```text
책
↓
장
↓
절
```

대규모 경전:

```text
초기경전 쉬운 해설
↓
잡아함경
↓
제1권
↓
제1경
↓
해설
```

경전 하나마다 별도의 웹사이트를 만들지 않는다.

---

# 12. 폴더명 규칙

웹 시스템과 URL에 직접 연결되는 폴더는 가능하면 영문 소문자와 하이픈을 사용한다.

예:

```text
buddhism
agama
samyukta-agama
madhyama-agama
linji
huangbo
jung
hawkins
bailey
```

기본 규칙:

```text
영문 소문자
단어 구분 = -
공백 사용하지 않음
불필요한 특수문자 사용하지 않음
```

예:

```text
madhyama-agama
```

권장하지 않음:

```text
Madhyama Agama
중아함경 웹북
madhyama_agama_final
```

---

# 13. 일반 MD 파일명 규칙

가장 기본적인 형식은 다음과 같다.

```text
번호_제목.md
```

예:

```text
01_서론.md
02_에고의_구조.md
03_지각과_의식.md
```

파일 수가 많아질 가능성이 있으면 처음부터 자릿수를 통일한다.

---

# 14. 번호 자릿수 규칙

### 100개 미만

```text
01
02
03
...
99
```

### 100개 이상

```text
001
002
003
...
120
```

### 1,000개 이상

```text
0001
0002
0003
...
1300
```

한 웹북 안에서는 번호 자릿수를 통일한다.

권장하지 않음:

```text
1
02
003
14
```

---

# 15. 장·절 형식

책이 다음 구조라면:

```text
1.1
1.2
1.3
2.1
2.2
```

표시 제목은 그대로 유지한다.

예:

```text
1.1 글래머 문제의 전체 윤곽
```

하지만 발행 파일명은 다음처럼 만든다.

```text
01-01_글래머_문제의_전체_윤곽.md
01-02_정신계의_왜곡_일루전.md
02-01_글래머의_전체_구조.md
```

즉:

```text
표시 번호 = 1.1
파일 번호 = 01-01
```

로 구분한다.

---

# 16. 대규모 경전 파일명

잡아함경처럼 1,000개 이상 존재하는 자료는 네 자리 번호를 기본으로 한다.

예:

```text
0001_제1경.md
0002_제2경.md
0003_제3경.md
...
1300_제1300경.md
```

경 이름이 확정되어 있다면:

```text
0001_선법경.md
0002_주도수경.md
```

처럼 제목을 포함한다.

---

# 17. 권별 폴더가 있는 경우

대형 경전은 권 단위 폴더를 사용할 수 있다.

예:

```text
samyukta-agama/
│
├─ 01/
│  ├─ 0001_제1경.md
│  ├─ 0002_제2경.md
│  └─ ...
│
├─ 02/
│  ├─ 0033_제33경.md
│  ├─ 0034_제34경.md
│  └─ ...
│
└─ ...
```

권이 바뀌어도 경 번호를 다시 0001부터 시작하지 않는다.

책 전체에서 고유 번호를 유지한다.

---

# 18. 파일명의 공백

파일명에서는 공백 대신 `_`를 사용한다.

권장:

```text
01_글래머의_본질.md
```

권장하지 않음:

```text
01 글래머의 본질.md
```

---

# 19. 파일명에서 제거할 특수문자

다음 문자는 발행 파일명에서 사용하지 않는 것을 원칙으로 한다.

```text
/ \ : * ? " < > | # %
```

장식용 문장부호도 가능하면 제거한다.

예:

```text
「 」
『 』
‘ ’
“ ”
```

원래 제목:

```text
4.6 융합과 ‘퍼스낼리티의 소멸’의 정확한 의미
```

발행 파일명:

```text
04-06_융합과_퍼스낼리티의_소멸의_정확한_의미.md
```

본문 제목에서는 원래 문장부호를 유지해도 된다.

---

# 20. 파일명과 문서 제목은 같을 필요가 없다

파일명은 시스템 관리용이다.

본문의 `# 제목`은 독자에게 보이는 정식 제목이다.

예:

파일:

```text
04-06_융합과_퍼스낼리티의_소멸의_정확한_의미.md
```

본문:

```markdown
# 4.6 융합과 ‘퍼스낼리티의 소멸’의 정확한 의미
```

이 둘을 구분한다.

---

# 21. 발행 후 파일명은 영구 ID처럼 취급한다

한번 공개된 웹북 파일명과 경로는 특별한 이유가 없으면 변경하지 않는다.

예:

```text
0001_선법경.md
```

가 공개된 뒤 제목 번역이 조금 바뀌었다고 해서:

```text
0001_좋은법에대한경.md
```

으로 변경하지 않는다.

필요하면 본문의 표시 제목만 수정한다.

이 원칙은 기존 링크와 검색 결과가 깨지는 것을 방지하기 위한 것이다.

---

# 22. 원본 파일명과 발행 파일명의 연결

가능하면 `book.yaml` 또는 별도의 메타데이터에서 원본 이름과 발행 정보를 추적할 수 있게 한다.

예:

```yaml
chapters:
  - id: "04-06"
    title: "융합과 ‘퍼스낼리티의 소멸’의 정확한 의미"
    file: "04-06_융합과_퍼스낼리티의_소멸의_정확한_의미.md"
    source_file: "『글래머_ 세계 문제』 제4부 「융합의 기법」__「4.6 융합과 ‘퍼스낼리티의 소멸’의 정확한 의미」.md"
```

이렇게 하면 나중에도 어떤 발행 파일이 어느 원본에서 왔는지 확인할 수 있다.

---

# 23. book.yaml 기본 표준

각 웹북에는 가능하면 `book.yaml`을 둔다.

예:

```yaml
title: 중아함경 쉬운 해설
category: 불교
collection: 초기경전 쉬운 해설
series: 중아함경

description: >
  불교 경전에 관심 있는 일반 독자가
  경전의 의미를 쉽게 이해하고
  불필요한 오해를 피할 수 있도록 돕는 해설.

language: ko
status: ongoing

chapters:
  - id: "0001"
    title: "선법경"
    file: "0001_선법경.md"

  - id: "0002"
    title: "주도수경"
    file: "0002_주도수경.md"
```

필요할 경우 다음 정보를 추가한다.

```yaml
author:
translator:
commentator:
source:
edition:
published:
updated:
```

실제 확인되지 않은 정보는 임의로 입력하지 않는다.

---

# 24. MD 본문 기본 규칙

Markdown 파일의 첫 번째 H1은 정식 문서 제목으로 한다.

예:

```markdown
# 제1경 선법경
```

그 아래에는 콘텐츠 성격에 따라 필요한 구조를 둔다.

예:

```markdown
# 제1경 선법경

## 원문

원문 내용

## 자연스러운 번역

번역 내용

## 빠른 이해

이 경에서 먼저 알아야 할 내용을 쉽게 설명한다.

## 상세 해설

본문의 의미와 전후 논리를 자세히 설명한다.

## 오해하기 쉬운 부분

잘못 이해하기 쉬운 내용을 설명한다.

## 핵심 정리

필요한 경우 핵심을 정리한다.
```

---

# 25. 모든 MD에 동일한 소제목을 강제하지 않는다

책과 자료의 성격이 다르므로 다음과 같은 소제목을 모든 파일에 억지로 넣지 않는다.

```text
원문
번역
빠른 이해
상세 해설
핵심 개념
오해하기 쉬운 부분
핵심 정리
```

필요한 항목만 사용한다.

웹북 시스템이 요구하는 최소 규칙은:

```text
# 정식 제목
본문
```

이다.

---

# 26. 프로젝트에서 이미 확정된 본문 구조를 우선한다

각 원고 프로젝트에서 이미 해설 형식이 확정되어 있다면 웹북 발행을 이유로 다시 고치지 않는다.

예를 들어 기존 해설이:

```text
제목
빠른 이해
원문
번역
상세 해설
오해 방지
호킨스 박사님의 언어로 보기
```

로 확정되어 있다면 그대로 보존한다.

웹북 표준은 콘텐츠를 획일화하기 위한 규칙이 아니라 **발행 구조를 표준화하기 위한 규칙**이다.

---

# 27. Markdown에는 웹사이트 코드를 넣지 않는다

원고에 웹사이트 디자인이나 탐색 코드를 직접 넣지 않는다.

예:

```html
<div class="next-page">
...
</div>
```

같은 코드를 각 MD에 넣지 않는다.

다음과 같은 네비게이션도 원칙적으로 웹북 엔진에서 생성한다.

```text
← 이전
목차
다음 →
```

Markdown은 콘텐츠에 집중한다.

---

# 28. Markdown과 웹사이트를 분리한다

기본 원칙:

```text
콘텐츠
= Markdown

목차·검색·디자인·이동
= 웹북 시스템
```

이렇게 해야 나중에 웹북 디자인을 전면 변경해도 수백 개 또는 수천 개의 Markdown 파일을 수정할 필요가 없다.

---

# 29. 기존 본문 보존

웹북 발행은 콘텐츠 편집과 별개의 작업이다.

사용자가 내용 수정을 요청하지 않았다면 다음을 임의로 변경하지 않는다.

- 문장
- 표현
- 번역
- 해설
- 인용
- 제목
- 문단
- 소제목
- 번호
- 설명량
- 문체

필요한 경우 발행용 파일명·폴더·메타데이터만 조정한다.

---

# 30. 웹북 화면 공통 규격

모든 웹북에서 다음 요소를 기본적으로 제공한다.

- 책 제목
- 현재 장·경·절 제목
- 전체 목차
- 현재 위치
- 이전 문서
- 다음 문서
- 모바일 대응
- 긴 글에 적합한 본문 폭
- 적절한 글자 크기
- 적절한 행간

가능하면 다음도 제공한다.

- 전체 검색
- 목차 접기
- 다크 모드
- 읽는 위치 표시
- 상단으로 이동
- 현재 페이지 공유

디자인의 화려함보다 긴 글의 가독성을 우선한다.

---

# 31. URL 구조

가능한 한 의미 있는 계층을 사용한다.

예:

```text
/buddhism/
/buddhism/agama/
/buddhism/agama/samyukta/
/buddhism/agama/madhyama/

/buddhism/zen/linji/
/jung/
/bailey/glamour/
```

기존 공개 URL이 있다면 새 표준에 맞추기 위해 무리하게 변경하지 않는다.

---

# 32. 새 웹북 발행 절차

새 책은 다음 순서로 처리한다.

```text
1. 원본 Markdown 수집
2. 발행 대상 파일 확인
3. 발행용 복사본 준비
4. 파일명 자동 정규화
5. 책 폴더 생성
6. book.yaml 생성
7. 목차 순서 확인
8. GitHub 반영
9. 웹북 빌드
10. 이전/다음 링크 확인
11. 모바일 확인
12. 공개 URL 확인
13. Notion 도서관 등록
14. Drive 발행본 백업
```

---

# 33. 기존 웹북에 새 파일 추가

기존 책은 다음 정도의 과정으로 끝나야 한다.

```text
새 원본 Markdown 전달
↓
발행 파일명 자동 결정
↓
발행용 복사본 생성
↓
book.yaml 목차 추가
↓
GitHub 반영
↓
자동 웹북 빌드
```

기존 URL이 유지되면 Notion을 다시 수정하지 않는다.

---

# 34. 발행 자동화의 목표

궁극적으로 사용자는 파일명 규칙을 매번 지시할 필요가 없어야 한다.

사용자가:

> 이 MD 파일들을 중아함경 웹북에 추가해.

라고 하면 시스템은 자동으로:

```text
원본 파일 확인
↓
제목·번호 확인
↓
표준 발행 파일명 생성
↓
발행용 복사본 생성
↓
기존 목차에 추가
↓
GitHub 발행
↓
기존 URL 유지
```

까지 처리하는 것을 목표로 한다.

---

# 35. 원본 파일명의 자동 정규화 원칙

발행 단계에서는 가능한 한 다음 정보를 원본 MD에서 읽어 파일명을 결정한다.

우선순위:

```text
1. 문서에 명시된 공식 번호
2. H1 정식 제목
3. 확정 목차의 번호와 제목
4. 기존 파일명
```

예를 들어 원본 파일명이:

```text
붙여넣은 마크다운(7)(1).md
```

이어도 본문 첫 줄이:

```markdown
# 4.6 융합과 ‘퍼스낼리티의 소멸’의 정확한 의미
```

라면 발행본은 자동으로:

```text
04-06_융합과_퍼스낼리티의_소멸의_정확한_의미.md
```

로 만들 수 있다.

따라서 사용자가 원본 파일명을 미리 정리할 필요가 없다.

---

# 36. 자동 파일명 생성 시 절대 하지 말아야 할 것

다음은 피한다.

- 본문을 수정해서 파일명에 맞춤
- 확정되지 않은 번호를 임의로 추정
- 제목의 의미를 임의로 축약
- 같은 번호가 이미 있는데 덮어쓰기
- 기존 발행 파일을 무단 개명
- 원본 파일 삭제
- 원본 파일 자체를 발행 편의를 위해 수정

---

# 37. 동일 파일명 충돌 처리

같은 발행 파일명이 이미 있을 경우 덮어쓰지 않는다.

먼저 다음을 확인한다.

```text
같은 문서의 새 버전인가?
완전히 다른 문서인가?
기존 파일을 교체하라는 지시가 있었는가?
```

명확한 교체 지시가 없는 경우 기존 파일을 보호한다.

---

# 38. 버전 관리

원고는 필요에 따라 다음 상태를 가질 수 있다.

```text
초안
↓
1차 해설
↓
정제본
↓
최종본
↓
발행본
```

웹북에는 사용자가 발행 대상으로 확정한 버전을 사용한다.

파일명에 `최종`, `완성`, `수정`, `(1)`, `(2)` 등이 있다고 해서 자동으로 최신본이라고 판단하지 않는다.

---

# 39. 기존 웹북 보호 원칙

기존 웹북 수정 전에 다음을 확인한다.

1. 현재 공개 URL
2. 현재 저장소
3. 현재 경로
4. 기존 목차
5. 기존 파일
6. 새로 추가할 범위

별도 지시 없이 다음을 하지 않는다.

- 기존 책 삭제
- 기존 저장소 삭제
- 공개 URL 변경
- 대량 파일명 변경
- 본문 재작성
- 다른 책의 파일 덮어쓰기

---

# 40. 우선순위

규칙이 충돌하는 경우 다음 순서를 따른다.

1. 사용자의 최신 명시적 지시
2. 기존 발행본과 공개 URL 보존
3. 원본 콘텐츠 보존
4. 확정 목차·번호·순서 보존
5. 원본과 발행본의 대응 관계 보존
6. 웹북 표준 파일명
7. 관리 편의성
8. 디자인 개선

---

# 41. 최종 운영 원칙

이 시스템은 다음 네 역할을 명확히 분리한다.

> **각 프로젝트는 원고를 만든다.**

> **웹북 프로젝트는 원고를 발행 가능한 구조로 정규화한다.**

> **GitHub는 웹북을 출판한다.**

> **Notion은 독자가 웹북을 찾는 도서관 입구가 된다.**

Google Drive는 그 과정에서 원본과 발행본을 안전하게 보관하는 서고 역할을 한다.

---

# 42. 핵심 한 문장

> **원본 MD는 손대지 않는다. 웹북으로 가져오는 순간 발행용 복사본의 파일명·폴더·메타데이터만 자동으로 표준화한다.**

따라서 서로 다른 프로젝트에서 제각기 다른 이름으로 만들어진 Markdown 파일이라도 사용자가 일일이 파일명을 다시 정리할 필요가 없다.
