# 원본 정상본 재생성 정책

이 문서는 웹북 발행 과정에서 원본 자료가 조각·복구 파일 형태로만 남아 있을 때의 처리 원칙을 정한다.

## 핵심 원칙

웹북 발행 프로젝트는 **원본 콘텐츠를 복원·추정·재작성하는 곳이 아니다.**

다음과 같은 파일만 존재하는 경우 이를 억지로 합쳐 발행본을 만들지 않는다.

- 조각 파일
- recovery 파일
- B64 / base64 복구 데이터
- archive / payload 파일
- 중간 산출물
- 일부만 남은 불완전 Markdown
- 정상 문서인지 확인되지 않은 임시본

이 경우 해당 문서는 **`재생성 필요`** 상태로 표시하고 발행 대상에서 제외한다.

## 처리 흐름

```text
정상 원본 MD 없음
↓
조각·복구 파일 존재 확인
↓
웹북 발행 프로젝트에서는 재조립하지 않음
↓
해당 연구·해설 프로젝트에 재생성 작업으로 넘김
↓
원 프로젝트에서 정식 원문·기준 자료를 사용해 정상 MD 새로 생성
↓
내용 검증 및 파일 확정
↓
중앙 저장소 disnotons/webbooks에 직접 발행
↓
SUMMARY.md 갱신
↓
GitBook Git Sync
```

## 원 프로젝트의 역할

정상본이 없는 장·권·절은 **그 자료를 실제로 해설·작성하는 원 프로젝트에서 새로 생성한다.**

원 프로젝트는 기존 조각 파일을 단순 기계 결합하는 것이 아니라, 그 프로젝트의 정식 원문·소스·해설 규칙을 기준으로 완전한 정상 Markdown을 만든다.

새로 생성된 정상 MD가 확정되면 이후 발행본은 중앙 저장소를 기준으로 한다.

```text
disnotons/webbooks/books/<category>/<book>/
```

앞으로 별도의 중간 동기화 저장소나 복구 파이프라인은 만들지 않는다.

## 웹북 발행 프로젝트에서 하지 않는 일

- 조각 파일을 보고 빠진 문장을 추정
- 여러 recovery 파일을 임의로 합쳐 정상본이라고 판단
- B64 payload를 콘텐츠 기준본으로 승격
- 번호가 비었다는 이유만으로 빈 문서를 생성
- 불완전 파일을 SUMMARY.md에 등록
- 기존 정상 공개본을 조각 파일로 덮어쓰기
- 복구 자료를 임의 삭제

## 발행 가능 조건

다음 조건을 만족하는 문서만 정상 발행 대상으로 취급한다.

1. 독립된 정상 Markdown 파일이 존재한다.
2. 해당 프로젝트에서 내용이 완결된 문서로 확인된다.
3. H1·번호·범위가 식별 가능하다.
4. 기존 발행본과 충돌하지 않는다.
5. 필요한 경우 원본과 발행본의 동일성을 검증할 수 있다.

## 기존 조각·복구 파일 보호

조각·복구 파일은 정상본 생성 전까지 참고 자료로 보존한다.

정상본이 새로 만들어졌더라도 별도의 삭제 지시가 없으면 기존 복구 파일을 삭제하지 않는다.

## 운영 문장

> **정상 원본이 없으면 웹북 프로젝트에서 복구하지 않는다. 원 프로젝트에서 정상 MD를 새로 만든 뒤 중앙 webbooks에 직접 발행한다.**
